#include <bits/stdc++.h>

using namespace std;

int main()
{
    multiset<int> h;
    int x,n;
    cin>>n;
    for(int i=1;i<=n;i++)
    {
        cin>>x;
        h.insert(x);
    }
    multiset <int> :: iterator it1 = h.begin();
    multiset <int> :: iterator it2 = h.end();
    it2--;
    cout<<*it1<<" "<<*it2;
    return 0;
}
