#include <bits/stdc++.h>
using namespace std;
unsigned long long n,v[1005];
int main()
{
    cin>>n;
    for(int i=1;i<=n;i++) cin>>v[i];
    for(int i=1;i<=n;i++)
    {
        if(v[i] & 1)
            cout<<1<<" ";
        else
            cout<<0<<" ";
    }
    return 0;
}
